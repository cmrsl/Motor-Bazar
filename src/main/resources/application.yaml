# =====================
# Server Properties
# =====================
server:
  port: 8080  # Server port

# =====================
# Spring Application Properties
# =====================
spring:
  application:
    name: Car-Bazar  # Application name
    shutdown: graceful
    forward-headers-strategy: framework

  # Database Configuration
  datasource:
    url: jdbc:postgresql://localhost:5432/car_bazaar_db  # Database URL
    username: postgres  # Database username
    password: vijayk    # Database password
    driver-class-name: org.postgresql.Driver  # JDBC driver
    hikari:
      pool-name: HikariPool
      maximum-pool-size: 5
      minimum-idle: 1
      idle-timeout: 600000
      max-lifetime: 1800000
      connection-timeout: 30000
      initialization-fail-timeout: 60000

  jpa:
    open-in-view: false
    show-sql: false
    hibernate:
      ddl-auto: update   # ✅ production-safe
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        jdbc:
          time_zone: UTC
        format_sql: false
        generate_statistics: false
  # Multipart File Upload Configuration
  servlet:
    multipart:
      enabled: true  # Enable multipart uploads
      max-file-size: 10MB
      max-request-size: 10MB

  # Common Recommended Properties
  main:
    lazy-initialization: true
    allow-bean-definition-overriding: true  # Allow bean overriding (useful for dev)
  jackson:
    serialization:
      INDENT_OUTPUT: true  # Pretty print JSON responses

  mail:
    host: smtp-relay.brevo.com    # SMTP server host
    port: 587               # SMTP server port
    username: 9e0505001@smtp-brevo.com
    password: R5yFMUSI1qrAZnk8  # SMTP server password
    MAIL_FROM: motorbazaap.mp@gmail.com  # Default "from" email address
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          connectiontimeout: 10000
          timeout: 10000
          writetimeout: 10000

cloudinary:
  cloud-name: dplxse1ik
  api-key: 418686174868718
  api-secret: Kv5ojJwIeGfPLUE896Qu-NkAvHI



# =====================
# File Upload Configuration
# =====================
servlet:
  multipart:
    max-file-size: 10MB      # Maximum file size for uploads
    max-request-size: 10MB   # Maximum request size for uploads



# =====================
# Application Custom Properties
# =====================
app:
  storage:
    location: ./uploads  # File storage location


usersvc:
  otp:
    digits: 6               # Number of digits in OTP
    expiry-minutes: 10  # OTP expiration time in minutes
  rate-limit:
    resend-min-interval-seconds: 60  # Minimum interval between OTP resends
    max-sends-per-hour: 5          # Maximum OTP sends per hour per user
  jwt:
    secret: a2b4c6d8e0f1g3h5j7k9l1m2n3p4q5r6  # JWT secret key
    issuer: Car-Bazar          # JWT issuer
    access-exp-minutes: 120      # JWT access token expiration time in minutes
  channels:
    email-enabled: true  # Enable email channel for OTP
    sms-enabled: false   # Disable SMS channel for OTP
  sms:
    provider: MSG91  # SMS provider
    msg91:
      auth-key: your_msg91_auth_key  # MSG91 authentication key
      flow-id: your_msg91_flow_id    # MSG91 flow ID
      sender-id: CARBAZ  # MSG91 sender ID
      dlt-te-id: your_dlt_template_id  # DLT template ID for MSG91
      real-time-response: true  # Disable real-time response for MSG91
      timeout-ms: 5000          # Timeout for MSG91 requests in milliseconds






# =====================
# Logging Configuration
# =====================
logging:
  level:
    root: INFO
    org.springframework.security: DEBUG  # Debug security logs



# =====================
# End of Configuration
# =====================
